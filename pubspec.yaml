name: user_comments_assignment
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.2.0
  provider: ^6.1.2
  firebase_auth: ^5.1.2
  google_fonts: ^6.2.1
  fluttertoast: ^8.2.6
  firebase_remote_config: ^5.0.3
  cloud_firestore: ^5.1.0
  firebase_analytics: ^11.2.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
